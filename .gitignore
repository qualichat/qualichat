# Chat file
_chat.txt

# Test corpus of real chats (privacy — never commit)
docs/chats/

# Personal thesis report (private research data)
docs/YLAI_V2.pdf

# Source PDFs of target charts — same private data as the thesis report
# (cycled through different cuts). PNGs are derivable; the analysis doc
# in docs/graficos-analysis.md captures the structural insights without
# the raw data.
docs/graficos/

# Logo source assets — the production PNGs live in qualichat/ui/assets/.
# This folder holds the original .ai/.eps/.pdf + the upstream zip and
# macOS unzip cruft (__MACOSX/), none of which need to be in git.
docs/logo/

# Working files
.venv
.vscode
.project
.mypy_cache

# Documentation files/folders
docs/_build

# Setuptools building folders
build
dist
*.egg-info

# Python folders
__pycache__

# Test caches
.pytest_cache

# Docs files
notes.txt

# Obsidian vault — cerebro local do projeto (notas privadas de pesquisa)
# Estrutura, scripts e skills (.claude/) sao commitaveis; conteudo do vault nao.
obsidian_vault/
!obsidian_vault/README.md
!obsidian_vault/CLAUDE.md
.claude/settings.local.json